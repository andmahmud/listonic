import 'package:flutter/material.dart';

class Trush extends StatelessWidget {
  const Trush({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Trush"),
    );
  }
}